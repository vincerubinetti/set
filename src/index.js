import React from 'react';
import { render } from 'react-dom';
import { App } from './app.js';
// import { ReadmeImages } from './readme-images.js';

// run app
render(<App />, document.getElementById('root'));

// render cards needed for readme
// render(<ReadmeImages />, document.getElementById('root'));
